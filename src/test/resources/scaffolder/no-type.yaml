#%RAML 0.8
---
title: Sample API
baseUri: http://localhost/api
version: v1
/songs:
    displayName: Songs
    get:
        responses:
            200:
              body:
                application/json:
                  schema: |
                    {
                      "$schema": "http://json-schema.org/draft-03/schema",
                      "id": "http://jsonschema.net",
                      "required": true
                    }
